import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.arenap.app',
  appName: 'آرناپ',
  webDir: 'out',  
  server: {
    androidScheme: 'https'
  }
};

export default config;
