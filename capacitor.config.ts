import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.arenap.app',
  appName: 'آرناپ',
  webDir: 'out',  
  server: {
    androidScheme: 'https'
  }
};

export default config;
